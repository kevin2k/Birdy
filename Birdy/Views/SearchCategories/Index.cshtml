@model Birdy.Models.CategoryViewModels.SearchViewModel

@{
    ViewData["Title"] = "Index";
}

<br />
<h2>Index</h2>
<br />

<div class="form-border">
    <form asp-controller="SearchCategories" asp-action="Index">
        <div class="col-3">
            <label asp-for="SearchEntry.SearchText" class="custom-label"></label>
        </div>
        <div class="col-3">
            <input asp-for="SearchEntry.SearchText" class="form-control" />
        </div>
        <br />
    </form>
</div>





<!--
<div class="form-border">
    <div class="col-3">
        <label asp-for="SearchEntry.SearchText" class="custom-label"></label>
    </div>

    <div class="col-5">
        <input type="text" name="SearchString" />
    </div>

    <br />
    <div class="col-5">
        <input type="submit" value="Suche" class="btn btn-primary" />
    </div>

</div>
    -->
<br />

@Html.Partial("_CreateButtonPartial")
<div class="form-border">
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(m => m.Category.Name)
            </th>
            <th>
                @Html.DisplayNameFor(m => m.Category.CategoryGroup.Name)
            </th>
            <th></th>
            <th></th>
        </tr>
        @foreach (var item in Model.CategoriesIEnumerable)
        {
            <tr>
                <td>
                    @Html.DisplayFor(m => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(m => item.CategoryGroup.Name)
                </td>
                <td>
                    @Html.Partial("_TableButtonPartial", item.Id)
                </td>
            </tr>
        }

    </table>
</div>
